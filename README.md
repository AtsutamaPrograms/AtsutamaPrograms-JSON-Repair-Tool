# 🚀 AtsutamaPrograms JSON Repair Tool

[![GitHub license](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![GitHub release](https://img.shields.io/github/v/release/AtsutamaPrograms/AtsutamaPrograms-JSON-Repair-Tool?color=brightgreen)](https://github.com/AtsutamaPrograms/AtsutamaPrograms-JSON-Repair-Tool/releases/tag/Ver1.0.0)

## 🇯🇵 概要

**AtsutamaPrograms JSON Repair Tool** は、崩れたJSONデータを自動で修正し、整形・縮小するための**ブラウザベースのユーティリティ**です。末尾のカンマ、引用符のないキー、コメントなど、標準JSONでは許可されない構文エラーを修正し、有効なJSONに変換します。

### ✨ 主な機能

* **自動修正**: 開発者が遭遇しがちなJSONの構文エラーをワンクリックで修正します。
* **整形・縮小**: 読みやすくするための整形（インデント）と、転送に適した縮小（Minify）をサポート。
* **リアルタイムフィードバック**: 入力データの有効性を即座にチェックし、視覚的にフィードバックを提供します。
* **多機能**: コピー、ペースト、クリア、テーマ切り替え、多言語（日/英）対応。

### 🛠️ 使用方法 (クイックスタート)

1.  入力パネルに、修正したいJSON、またはJSONライクなテキストを貼り付けます。
2.  「**✨ Auto Fix**」ボタンを押すと、自動で構文エラーが修正され、整形されたJSONが出力パネルに表示されます。
3.  有効なJSONであれば、「**Format**」や「**Minify**」で表示形式を変更できます。
4.  「**📋 Copy**」ボタンで結果をクリップボードにコピーします。

---

## 🇬🇧 Overview

The **AtsutamaPrograms JSON Repair Tool** is a **browser-based utility** that automatically fixes, formats, and minifies broken JSON data. It corrects syntax errors not permitted in standard JSON, such as trailing commas, unquoted keys, and comments, converting them into valid JSON structures.

### ✨ Key Features

* **Auto Fix**: Corrects common JSON syntax errors encountered by developers with a single click.
* **Format & Minify**: Supports pretty-printing (indentation) for readability and minification for efficient transfer.
* **Real-time Feedback**: Instantly checks the validity of the input data and provides visual feedback.
* **Utility**: Includes Copy, Paste, Clear, Theme Toggle, and Multilingual (Japanese/English) support.

### 🛠️ Quick Start Guide

1.  Paste the JSON or JSON-like text you want to fix into the input panel.
2.  Click the "**✨ Auto Fix**" button to automatically correct syntax errors and display the formatted JSON in the output panel.
3.  If the JSON is valid, you can use "**Format**" or "**Minify**" to change the display style.
4.  Click the "**📋 Copy**" button to copy the result to the clipboard.

> ⚠️ README.mdを必ずお読みください / Please be sure to read the README.md.
